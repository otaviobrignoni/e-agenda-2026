@model DisplayFieldViewModel
<div class="d-flex justify-content-between align-items-start mb-2">
    <h5 class="card-title mb-0">@Model.Label</h5>
    <span class="text-muted small">@Model.Value</span>
</div>
